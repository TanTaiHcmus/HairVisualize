import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },
  headerButtonContainer: {
    flexDirection: "row",
    alignItems: "center",
  },
  headerButton: {
    color: "#fff",
    paddingHorizontal: 8,
  },
});
